# DAILY_LOG_2026-09-15.md

1. Se retomó el feature de autenticación + multi-tenancy en la sección 2 de `tasks.md` (Prisma schema and migration), con el plan ya esbozado el 2026-09-13.
2. Felipin decidió, para el backfill de `organizationId` (tarea 2.4), truncar las tablas `Tutor`/`Patient`/`Consultation` (y dependientes) en dev y test en vez de sembrar una `Organization` placeholder, ya que no hay datos reales todavía.
3. Se delegó la sección 2 completa al `coder`, con instrucción explícita de no commitear. Resultado: 3 migraciones (`Organization`/`User` reescrito/`RefreshToken`/`UserInvitation`/`PasswordResetToken`; `organizationId` nullable en Tutor/Patient/Consultation; `organizationId` requerido + índice + unique compuesto en `Tutor.rut`/`Tutor.email`), truncado verificado en ambas bases. `prisma migrate dev` no podía correr sin interacción por la advertencia de pérdida de datos del unique compuesto; se generó el SQL con `prisma migrate diff` y se aplicó con `migrate deploy`, mismo resultado.
4. El `coder` reportó que `tsc --noEmit` y los tests de `apps/api` quedan en rojo porque `tutors`/`patients`/`consultations` controllers todavía no setean/filtran `organizationId`, ahora requerido en los tipos de Prisma. Correctamente no tocó esos controllers (fuera del alcance de la sección 2; es el trabajo de las secciones 6-8) y se detuvo a reportar el conflicto en vez de decidir.
5. Se le presentó a Felipin la disyuntiva: aceptar `master` en rojo hasta cerrar las secciones 6-8, o fusionar el alcance de la sección 3 (auth infra) con las secciones 6-8 en la próxima fase para volver a verde antes. Felipin eligió aceptar el rojo temporal y seguir el orden de `tasks.md` tal cual está escrito.
6. Se marcaron 2.1-2.9 como hechas en `openspec/changes/add-auth-multitenancy/tasks.md` (2.4 con una nota aclarando que se truncó en vez de backfillear) y se actualizó `TASKS.md`, dejando registrado el estado rojo esperado. No se tocó `PROJECT_STATUS.md`: las tareas 11.1-11.3 de `tasks.md` ya son responsables de esa actualización al cerrar todo el cambio.
7. Próximo paso: sección 3 (Auth infrastructure) de `tasks.md`, fase por fase.
8. Se armó el plan de la sección 3 y se revisó con Felipin antes de delegar: layout de archivos (`lib/auth/*`, `lib/for-organization.ts`, `lib/email/*`, `middlewares/require-auth.ts`, `middlewares/require-role.ts`, `types/express.d.ts`), separado de `modules/auth/` (que se crea en la sección 4). Dos decisiones acordadas antes de delegar: dejar `RESEND_API_KEY` sin configurar (Felipin no tiene cuenta de Resend todavía; se prueba en las secciones 4-5) y usar SHA-256 en vez de bcrypt para hashear refresh/reset tokens, por ser strings aleatorios de alta entropía y no passwords elegidas por humanos.
9. Se delegó la implementación completa de 3.1-3.9 al `coder` (sin tests, sin endpoints, sin commit). Resultado: 11 archivos nuevos, ningún archivo existente modificado. `tsc`/tests quedaron en el mismo rojo preexistente de las secciones 6-8, sin errores nuevos. El coder tomó 4 decisiones de detalle no explícitas en `design.md`, presentadas a Felipin y aprobadas tal cual: TTL de 1 hora para el password reset token, `findFirst` en vez de `findUnique` para buscar por `tokenHash` (no tiene `@unique` en el schema), tipado `any` aislado al handler compartido de la extensión de Prisma (los tipos internos de `Prisma.defineExtension` no encajaban con un tipo escrito a mano), y `ScopedPrismaClient` derivado vía `typeof` de un cliente extendido creado solo para capturar el tipo (no ejecuta queries).
10. Antes de invocar al `tester`, se detectó que `apps/api/.env` no tenía `JWT_SECRET`, lo que hace fallar la carga de `access-token.ts` (y por lo tanto de `require-auth.ts`) en cualquier test que los importe. A diferencia de `RESEND_API_KEY`, no depende de una cuenta externa, así que el orquestador generó un valor aleatorio con `crypto.randomBytes(48)` y lo agregó directamente a `apps/api/.env` (gitignored, sin commit), con aprobación explícita de Felipin.
11. Se delegó al `tester` la escritura de unit tests para 3.1-3.9 (excluyendo 3.8's verificación end-to-end, diferida a la sección 5 por falta de cuenta de Resend). Resultado: 7 archivos de test nuevos, colocados junto a cada archivo fuente, 24 tests en total, todos en verde. No se encontraron bugs reales en la implementación. `tutor-patient.api.test.ts` sigue fallando por el problema preexistente y conocido de las secciones 6-8, sin relación con este trabajo.
12. Se marcaron 3.1-3.9 como hechas en `openspec/changes/add-auth-multitenancy/tasks.md` y se actualizó `TASKS.md`. No se tocó `PROJECT_STATUS.md`: la sección 3 no expone todavía ninguna capacidad nueva (no hay endpoints), así que no hay una decisión de arquitectura o capacidad que documentar ahí por ahora.
13. Próximo paso: sección 4 (Auth endpoints) de `tasks.md`.
14. Se armó el plan de la sección 4 y se revisó con Felipin antes de delegar: `apps/api/src/modules/auth/{auth.schemas,auth.controller,auth.routes}.ts` con el patrón de tres archivos ya usado en `tutors`/`patients`. Tres decisiones acordadas antes de delegar: password mínimo de 8 caracteres, link de recuperación provisional como `${APP_URL}/reset-password?token=...` (env var placeholder, sin definir todavía el deep link real de Expo — se deja para la sección 10 de mobile), y `logout` público (el refresh token en el body ya identifica la sesión) con `logout-all` protegido por `requireAuth`.
15. Se delegó 4.1-4.9 al `coder` (sin tests, sin commit). Resultado: 3 archivos nuevos en `modules/auth/`, `email-sender.ts` extendido con `sendPasswordResetEmail`, `routes/index.ts` actualizado, `APP_URL` agregada a `apps/api/.env`. `tsc --noEmit` sin errores nuevos en el módulo auth (los preexistentes de `tutors`/`patients`/`consultations` siguen igual, esperado).
16. Se delegó al `tester` la escritura de tests de integración HTTP para los 7 endpoints. El `tester` encontró un bug real y se detuvo a reportarlo en vez de arreglarlo: `apps/api/src/lib/email-sender.ts` (el archivo que el `coder` de la sección 4 extendió, indicado por el orquestador al armar el plan) resultó ser un placeholder huérfano de la sección 1 (tarea 1.2) — la implementación real de la sección 3 (tarea 3.8) vivía sin usar en `apps/api/src/lib/email/{email-sender.ts,resend-email-sender.ts}` desde el commit de esa fase. Ambas implementaciones construían el cliente Resend de forma eager al cargar el módulo; al quedar `modules/auth/auth.controller.ts` importado desde `app.ts`, la ausencia de `RESEND_API_KEY` (todavía no hay cuenta de Resend) hacía crashear la carga de toda la app, rompiendo también el test preexistente `tutor-patient.api.test.ts` que antes pasaba.
17. El error de apuntar al archivo equivocado fue del orquestador (un Glob que no bajaba a subcarpetas se saltó la implementación real al armar el plan), no del `coder`. Se reinvocó al `coder` para consolidar en la implementación real de `lib/email/`, hacer la construcción del cliente Resend perezosa (getters memoizados, sin validar env vars ni instanciar `Resend` al importar), eliminar el archivo duplicado, y agregar `EMAIL_FROM` a `.env`. Verificado de forma independiente por el orquestador: 17/17 tests de auth en verde, y `tutor-patient.api.test.ts` vuelve a cargar y correr (sus 29 fallos restantes son el problema preexistente y ya documentado de `organizationId` en tutors/patients/consultations, sin relación con este fix).
18. Se marcaron 4.1-4.9 como hechas en `openspec/changes/add-auth-multitenancy/tasks.md` y se actualizó `TASKS.md`. No se tocó `PROJECT_STATUS.md`: igual que en la sección 3, su actualización queda para las tareas 11.1-11.3 al archivar el cambio completo.
19. Próximo paso: sección 5 (User invitations) de `tasks.md`.
20. Se armó el plan de la sección 5 y se revisó con Felipin antes de delegar: `apps/api/src/lib/auth/invitation-token.ts` (mirror de `password-reset-token.ts`) y nuevo módulo `apps/api/src/modules/users/` para `POST /api/users/invite`; `POST /api/auth/activate` se agrega al módulo `auth` existente. Cuatro decisiones acordadas antes de delegar: `User.passwordHash` del invitado pendiente se setea con el hash de un valor aleatorio inutilizable (el schema no permite `passwordHash` nulo); TTL de 7 días para el token de activación; `role` del invite como `z.literal("OWNER")` (único valor de `UserRole` hoy); y centralizar la guardia de `APP_URL` en un nuevo `apps/api/src/lib/env.ts` en vez de duplicarla, ya usada por dos controllers.
21. Felipin agregó un requisito antes de aprobar: la validación del token, marcar `acceptedAt`, y actualizar el `passwordHash` del usuario deben ser atómicos (si la actualización del usuario falla, `acceptedAt` no debe quedar marcado). Se rediseñó `invitation-token.ts` para que `activateInvitation()` haga las tres operaciones dentro de un único `prisma.$transaction`, con el hash de la contraseña (bcrypt, costo de CPU) calculado *antes* de abrir la transacción para no mantenerla abierta de más.
22. Se delegó 5.1-5.4 al `coder` (sin tests, sin commit). Resultado: `lib/env.ts`, `lib/auth/invitation-token.ts`, módulo `modules/users/` y las adiciones a `modules/auth/` nuevos; ninguna desviación del plan. `tsc --noEmit` y los tests sin errores nuevos (los preexistentes de `tutors`/`patients`/`consultations`, esperados hasta las secciones 6-8, siguen igual). Verificado de forma independiente por el orquestador leyendo el diff completo.
23. Felipin pidió al `tester` cubrir explícitamente: email de invitación con recipiente y URL correctos, solo `OWNER` puede invitar, `organizationId` nunca del body, token válido/expirado/aceptado/inexistente, el cliente no puede alterar `role`/`organizationId` en la activación, el caso de atomicidad del punto 21, y un E2E de camino feliz completo (invita → activa → loguea). Se delegó con ese listado más casos de seguridad adicionales (la contraseña nunca viaja en el email, el usuario pendiente no puede loguear antes de activar).
24. Resultado del `tester`: `apps/api/src/modules/users/users.api.test.ts` (nuevo, 5 tests) y adiciones a `apps/api/src/modules/auth/auth.api.test.ts` (8 tests de activación + 1 E2E), incluyendo el caso de atomicidad (se borra el `User` pendiente antes de activar, confirmando que `acceptedAt` queda en `null` tras el rollback). 30/30 tests en verde, sin bugs reales encontrados en la sección 5. Verificado de forma independiente por el orquestador corriendo `vitest` acotado a esos dos archivos.
25. Se marcaron 5.1-5.4 como hechas en `openspec/changes/add-auth-multitenancy/tasks.md` y se actualizó `TASKS.md`. No se tocó `PROJECT_STATUS.md`: su actualización sigue diferida a las tareas 11.1-11.3, al archivar el cambio completo.
26. Próximo paso: sección 6 (Tenant isolation — Tutors) de `tasks.md`.
