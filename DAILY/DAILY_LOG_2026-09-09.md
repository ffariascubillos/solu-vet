# DAILY_LOG_2026-09-09.md

1. Felipin respondió las 10 decisiones de arquitectura planteadas el día anterior: rol `OWNER` único por ahora (también para el veterinario independiente), unicidad de Tutor por organización, prioridad estricta en el aislamiento cross-tenant, estrategia JWT de access token corto + refresh token, invitación de usuarios vía enlace de correo (sin contraseñas en texto plano), `organizationId` nunca confiado desde el cliente, `Species`/`Breed`/`Region` siguen globales, y reescritura limpia de `User` aprobada.
2. Corrió `/opsx:propose` y creó el change de OpenSpec `add-auth-multitenancy` (`openspec/changes/add-auth-multitenancy/`) con `proposal.md`, cuatro capacidades nuevas (`organizations`, `user-authentication`, `user-invitations`, `tenant-data-isolation`), `design.md` (estrategia de tokens, duraciones, elección de hashing, aislamiento en capas verificado contra la documentación actual de Prisma vía context7, estrategia de invitación por correo, decisión de campos SaaS-ready en `Organization`) y `tasks.md` (11 grupos de tareas). Validado limpio con `openspec validate`.
3. Felipin preguntó por qué no había recuperación de contraseña; se coincidió en que era un vacío real (un veterinario independiente, único `OWNER`, quedaría bloqueado sin ella) y se sumó al mismo change: nuevos requisitos en `specs/user-authentication/spec.md`, una decisión de modelo `PasswordResetToken` en `design.md`, y tareas nuevas en `tasks.md`. Revalidado limpio.
